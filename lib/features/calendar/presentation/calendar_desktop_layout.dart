/// Desktop layout breakpoints for Calendar M6 (not mobile M9).
abstract final class CalendarDesktopLayout {
  /// Content width at or above this = normal desktop density.
  static const double narrowBreakpoint = 1100;
}
